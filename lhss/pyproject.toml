[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
authors = ["tsaloligeorgia <gtsaloli@gmail.com>"]
description = ""
name = "vhssstemp"
version = "0.1.0"
[tool.poetry.dependencies]
python = "^3.7"
sage = "^0.0.0"
[tool.poetry.dev-dependencies]
